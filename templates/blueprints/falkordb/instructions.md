1. Open the generated domain to access FalkorDB Browser.
2. Create a connection using `localhost` as the host and `6379` as the port.
3. Leave the username empty and use the generated `FALKORDB_PASSWORD` from the Dokploy environment settings.
4. Keep port `6379` private unless remote database clients need direct access. The browser interface can access it inside the container.
