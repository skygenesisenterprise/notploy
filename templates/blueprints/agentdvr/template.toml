[variables]
main_domain = "${domain}"
timezone = "America/New_York"

[config]
mounts = []
[[config.domains]]
serviceName = "agentdvr"
port = 8090
host = "${main_domain}"

[config.env]
TIMEZONE = "${timezone}"
