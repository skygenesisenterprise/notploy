version: "3.8"

services:
  chroma:
    image: chromadb/chroma:1.5.6
    restart: unless-stopped
    expose:
      - "8000"
    environment:
      - ANONYMIZED_TELEMETRY=FALSE
    volumes:
      - chroma-data:/data

volumes:
  chroma-data:
