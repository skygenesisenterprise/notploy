# template.toml
[variables]
main_domain = "${domain}"

[config]
mounts = []
[[config.domains]]
serviceName = "opengist"
port = 6157
host = "${main_domain}"

[config.env]
UID = "1001"
GID = "1001"
OG_LOG_LEVEL = "info"
