[variables]
main_domain = "${domain}"
docker_socket = "/var/run/docker.sock"

[config]
mounts = []
[[config.domains]]
serviceName = "dozzle"
port = 8080
host = "${main_domain}"

[config.env]
DOZZLE_USERNAME = "${username}"
DOZZLE_PASSWORD = "${password:16}"
