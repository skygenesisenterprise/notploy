version: "3.8"
services:
  spacetimedb:
    image: clockworklabs/spacetime:v2.6.1
    restart: unless-stopped
    command: start
    expose:
      - 3000
    volumes:
      - spacetimedb-data:/home/spacetime

volumes:
  spacetimedb-data: {}
