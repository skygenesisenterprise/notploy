[variables]
main_domain = "${domain}"
postgres_password = "${password:32}"  

[config]
mounts = []
[[config.domains]]
serviceName = "budget-board-client" 
port = 6253  
host = "${main_domain}"  
path = "/"

[config.env]
POSTGRES_PASSWORD = "${postgres_password}" 
