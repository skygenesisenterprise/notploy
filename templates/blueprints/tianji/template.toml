[variables]
main_domain = "${domain}"
# API Key: Used for session security.
jwt_secret = "${password:32}"

[config]
mounts = []
[[config.domains]]
serviceName = "tianji"
port = 12345
host = "${main_domain}"
